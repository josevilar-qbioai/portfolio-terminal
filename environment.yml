name: cartera
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pyyaml>=6.0
      - yfinance>=0.2.40
      - textual>=0.80.0
      - rich>=13.0
      - requests>=2.31
